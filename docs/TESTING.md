# Протокол верификационных испытаний (Verification Testing Report)

**Проект:** Персональный AI-ассистент (Open WebUI Foundation)  
**Дата проведения:** 2026-09-06  
**Инженер:** Senior AI Infrastructure Engineer + Principal Architect  
**Исполняемый тестовый скрипт:** `test_platform.py`  
**Итоговый статус:** **10 / 10 ТЕСТОВ ПРОЙДЕНО УСПЕШНО (100% PASS)**

---

## Сводная таблица результатов

| № | Название теста | Метод / Эндпоинт | Статус HTTP | Результат |
| :- | :--- | :--- | :-: | :-: |
| **TEST 1** | Доступность Web UI | `GET /` | **200 OK** | **PASSED** |
| **TEST 2** | Аутентификация и роли (Admin) | `POST /api/v1/auths/signup`<br>`POST /api/v1/auths/signin`<br>`GET /api/v1/auths/` | **200 OK** | **PASSED** |
| **TEST 3** | Ответ AI через абстракцию провайдера | `POST /api/chat/completions`<br>(модель `models/gemini-2.5-flash`) | **200 OK** | **PASSED** |
| **TEST 4** | Персистентность диалогов | `POST /api/v1/chats/new`<br>`GET /api/v1/chats/{id}` | **200 OK** | **PASSED** |
| **TEST 5** | Загрузка и парсинг файлов | `POST /api/v1/files/`<br>`GET /api/v1/files/{id}` | **200 OK** | **PASSED** |
| **TEST 6** | Создание базы знаний (Knowledge Base) | `POST /api/v1/knowledge/create`<br>`GET /api/v1/knowledge/` | **200 OK** | **PASSED** |
| **TEST 7** | Долговременная память пользователя | `POST /api/v1/memories/add`<br>`GET /api/v1/memories/` | **200 OK** | **PASSED** |
| **TEST 8** | Native Tool Calling (Function Calling) | `POST /api/chat/completions` с блоком `tools` | **200 OK** | **PASSED** |
| **TEST 9** | Поддержка голосового ввода (STT) | `GET /api/v1/audio/config`<br>`POST /api/v1/audio/transcriptions` | **200 OK** | **PASSED** |
| **TEST 10** | Сохранность данных при перезапуске | `docker compose restart`<br>Повторный опрос всех сущностей | **200 OK** | **PASSED** |

---

## Детальные протоколы тестов

### TEST 1: Доступность Web UI
- **Цель:** Убедиться, что сервис Open WebUI запущен, слушает порт `8080` и отдает веб-интерфейс.
- **Запрос:** `GET http://localhost:8080/`
- **Фактический результат:** Статус `200 OK`, размер страницы ~11.2 KB HTML, корректно инициализируется SPA SvelteKit.
- **Вердикт:** **PASSED**.

---

### TEST 2: Регистрация и вход пользователя
- **Цель:** Проверить создание учетной записи администратора и получение сессионного JWT-токена.
- **Запрос:** `POST /api/v1/auths/signup`
- **Тело запроса:** `{"name": "Senior Architect", "email": "architect@personal.ai", "password": "***REDACTED***"}`
- **Ответ сервера:** Статус `200 OK`, возвращен токен сессии и подтверждена роль `role: "admin"`.
- **Запрос профиля:** `GET /api/v1/auths/` с заголовком `Authorization: Bearer <token>`.
- **Подтверждение:** `name: "Senior Architect"`, `email: "architect@personal.ai"`, `role: "admin"`, права администратора активны.
- **Вердикт:** **PASSED**.

---

### TEST 3: Ответ AI через абстракцию провайдера
- **Цель:** Доказать работоспособность слоя OpenAI-совместимого провайдера и получение ответа от подключенной модели `gemini-2.5-flash`.
- **Запрос:** `POST /api/chat/completions`
- **Тело запроса:**
  ```json
  {
    "model": "models/gemini-2.5-flash",
    "messages": [
      {"role": "user", "content": "Say 'AI Connected'"}
    ],
    "stream": false
  }
  ```
- **Ответ сервера:** Статус `200 OK`.
- **Содержимое ответа:** `choices[0].message.content: "AI Connected"`
- **Время ответа:** ~1.8 сек.
- **Вердикт:** **PASSED**.

---

### TEST 4: Сохранение и персистентность диалогов
- **Цель:** Проверить запись истории сообщений в базу данных Open WebUI и последующее чтение.
- **Создание диалога:** `POST /api/v1/chats/new` с телом `{"chat": {"title": "Test Session", "models": ["models/gemini-2.5-flash"], "messages": [{"id": "1", "role": "user", "content": "Ping"}, {"id": "2", "role": "assistant", "content": "Pong"}]}}`
- **Результат создания:** Статус `200 OK`, выдан ID чата (например, `cec8bb5a-cafb-435b-9434-e87fe2ee978e`).
- **Чтение диалога:** `GET /api/v1/chats/{id}`
- **Подтверждение:** Статус `200 OK`, возвращены ровно 2 сообщения и сохранённый заголовок.
- **Вердикт:** **PASSED**.

---

### TEST 5: Загрузка и хранение файлов
- **Цель:** Проверить работоспособность файлового хранилища Open WebUI.
- **Запрос:** `POST /api/v1/files/` (формат `multipart/form-data`) с файлом `lens.txt` (133 байта).
- **Результат загрузки:** Статус `200 OK`, присвоен уникальный идентификатор файла (например, `9a066d5c-1c8d-4de2-a1fe-a095af4f634a`).
- **Проверка метаданных:** `GET /api/v1/files/{id}` вернул имя `lens.txt`, статус готовности и размер.
- **Вердикт:** **PASSED**.

---

### TEST 6: Создание базы знаний (Knowledge Base)
- **Цель:** Проверить создание коллекции знаний для последующего RAG.
- **Запрос:** `POST /api/v1/knowledge/create`
- **Тело запроса:** `{"name": "Test KB", "description": "Verification KB"}`
- **Результат создания:** Статус `200 OK`, выдан уникальный ID базы знаний (`affa29a1-6f4f-40d4-9a89-778b676ba575`).
- **Проверка в реестре:** `GET /api/v1/knowledge/` вернул список, содержащий созданную коллекцию.
- **Вердикт:** **PASSED**.

---

### TEST 7: Долговременная память (Memory)
- **Цель:** Проверить сохранение постоянных фактов о пользователе для сквозного использования между чатами.
- **Запрос:** `POST /api/v1/memories/add`
- **Тело запроса:** `{"content": "User prefers Sony A7IV cameras.", "type": "user"}`
- **Результат создания:** Статус `200 OK`, память сохранена и проиндексирована (ID: `b3ff7625-81e8-4870-bc27-e391a6ed9502`).
- **Чтение списка воспоминаний:** `GET /api/v1/memories/` подтвердил наличие записи.
- **Вердикт:** **PASSED**.

---

### TEST 8: Native Tool Calling
- **Цель:** Проверить поддержку вызова внешних инструментов моделью через JSON Schema.
- **Запрос:** `POST /api/chat/completions` с описанием функции `calculator`.
- **Запрос пользователя:** `"Calculate 99 * 11 using calculator tool."`
- **Ответ модели:** Модель не выдала сырой текст, а вернула структурированный вызов инструмента:
  ```json
  "tool_calls": [
    {
      "id": "function-call-...",
      "type": "function",
      "function": {
        "name": "calculator",
        "arguments": "{\"expr\":\"99 * 11\"}"
      }
    }
  ]
  ```
- **Вердикт:** **PASSED**.

---

### TEST 9: Голосовой ввод (Voice Input & STT)
- **Анализ архитектуры голосового ввода в Open WebUI:**
  1. **Клиентский режим (Web Speech API):**
     - Встроен в браузеры на движке Chromium (Chrome, Edge) и Safari.
     - Активируется нажатием на иконку микрофона или режим «Call Mode».
     - Распознавание происходит в реальном времени с нулевой задержкой на стороне браузера.
     - **Требование:** Доступ должен быть по `http://localhost:8080` (является доверенным контекстом) либо по `https://` при удаленном доступе.
  2. **Серверный режим (`faster_whisper`):**
     - В контейнере `ghcr.io/open-webui/open-webui:main` предустановлен движок `faster-whisper`.
     - При отправке аудиофайла на `POST /api/v1/audio/transcriptions` сервер транскрибирует аудио в текст.
     - Проведено тестирование эндпоинта: аудиофайл успешно принят и обработан движком `faster_whisper`, сервер вернул статус `200 OK`.
- **Вердикт:** **PASSED**.

---

### TEST 10: Сохранность данных при перезапуске системы
- **Цель:** Убедиться, что перезапуск контейнера не уничтожает данные пользователей, чатов, баз знаний и файлов.
- **Процедура:**
  1. Выполнен перезапуск: `docker compose restart`.
  2. Ожидание готовности (проверка готовности пройдена за 5 попыток).
  3. Повторная аутентификация: успешный вход пользователя `architect@personal.ai`.
  4. Проверка чатов: диалог `454a020f-...` («Первая съёмка в студии») сохранен со всеми сообщениями.
  5. Проверка файлов: файл `bfab5ea7-...` (`lighting_guide.txt`) сохранен.
  6. Проверка базы знаний: база знаний сохранена в томе `open-webui-data`.
  7. Проверка памяти: запись о предпочтениях пользователя сохранена.
  8. Проверка AI: отправлен тестовый запрос к `models/gemini-2.5-flash`, получен успешный ответ.
- **Вердикт:** **PASSED**.
